你是资深 SDET（测试开发），要在本工作区做一次“端到端联合测试”，输出可执行、可复现、可量化的结果。
重要约束：
- 默认不修改代码；如需改动必须先给出变更清单与风险，并征得确认后再执行
- 尽量使用只读验证与最小渲染；必要时可调用 CLI 运行渲染/脚本
- 产出一份结构化报告（JSON+人读摘要）

仓库要点：
- Python 工具脚本：tools/ 目录
- Remotion 工程：remotion_video/，组合 ID= TiebaStory
- 现有产物：projects/9461449190/**、output/**，props.9461449190.m6*.json

测试目标：
1) Python 脚本健壮性
   - 列出 tools/ 下 *.py；对每个支持 --help 的脚本执行 --help 并记录 exitCode=0
   - 重点执行：
     a) tools/tieba_m5_validate.py（若存在则校验 BGM 产物一致性）
     b) tools/tieba_m6_build_props.py 生成 props 临时文件（max-scenes=3）
2) Remotion CLI
   - 在 remotion_video 下执行：npx remotion compositions（应包含 TiebaStory）
   - 使用 props.9461449190.m6.v2.json 渲染最小视频到 output/9461449190.m6.test.mp4
   - 验证输出文件存在、大小>0
3) 产物一致性
   - 校验 output/9461449190.m6.v2.mp4 与 test 渲染文件均可读（仅文件存在与非零大小）
   - props 文件字段齐全：scenes[].image/audio/duration_s/subtitle 非空（前 3 条）
4) 结构化报告
   - 以 JSON 形式输出 {python:{...}, remotion:{...}, outputs:{...}, summary:{pass_count, fail_count, elapsed_s}}
   - 同时给出人类可读的“测试摘要”若干条（每条一句）

请按“步骤 -> 实际命令 -> 结果 -> 结论”的格式进行；命令失败时继续后续用例但计入失败。

